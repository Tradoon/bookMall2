<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tradoon.bookMall.dao.PmsProductAttributeCategoryMapper">
    <insert id="insertAttribute">
        insert into pms_product_attribute_category(
         <trim suffixOverrides=",">
             <if test="id!=null">
                 id,
             </if>
            <if test="name!=null">
                name,
            </if>
            <if test="paramCount!=null">
                param_count,
            </if>
            <if test="attributeCount!=null">
                attribute_count
            </if>
         </trim>
        )values (
        <trim suffixOverrides=",">
            <if test="id!=null">
                #{id},
            </if>
            <if test="name!=null">
                #{name},
            </if>
            <if test="paramCount!=null">
                #{paramCount},
            </if>
            <if test="attributeCount!=null">
                #{attributeCount}
            </if>
        </trim>
        )
    </insert>

    <select id="findByName" resultType="com.tradoon.bookMall.model.PmsProductAttributeCategory">
        select `id` ,
               `name` ,
               `attribute_count` ,
               `param_count`
        from pms_product_attribute_category
        where name=#{name}
        limit 1
    </select>
</mapper>